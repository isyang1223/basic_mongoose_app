<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Basic Mongoose App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="main.js"></script>
</head>

<body>

    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
            <h3>
                <%= errors[x].message %>
            </h3>
            <% } %>
                <% } %>


    <% for (var i in users) { %>
        <p><%= users[i].first_name %> <%= users[i].last_name %> <%= users[i].age %> <%= users[i].email %></p>
        <% } %>

    <form action="/users" method="POST">

        <p>First name: <input type = "text" name = "first_name" id = "name"> </p>
        <p>Last name:<input type="text" name="last_name" id="name"> </p>
        <p>age: <input type="text" name="age" id="age"></p>
        <p>email:<input type="text" name="email" id="age"></p>
        <input type = "submit">
    </form>


</body>

</html>